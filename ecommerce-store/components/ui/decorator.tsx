import Image from 'next/image';

const Decorator = () => {
  return (
    <div>
      <Image alt="Decorator" src="/decorator.svg" width={4} height={25} />
    </div>
  );
};

export default Decorator;
